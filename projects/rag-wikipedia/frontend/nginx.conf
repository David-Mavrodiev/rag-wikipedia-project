server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /query {
        proxy_pass http://api:8000/query;
    }

    location /health {
        proxy_pass http://api:8000/health;
    }
}
